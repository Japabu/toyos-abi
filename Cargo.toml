[package]
name = "toyos-abi"
version = "0.1.0"
edition = "2021"

[features]
rustc-dep-of-std = ["core"]

[dependencies]
core = { version = "1.0.0", optional = true, package = "rustc-std-workspace-core" }
